# Copy this file to .env and fill real values for local development.
# Note: App Runner ignores .env files. Configure the same variables in the service UI.

# Runtime
ASPNETCORE_ENVIRONMENT=Development
PORT=8080
ASPNETCORE_URLS=http://0.0.0.0:8080
DISABLE_HTTPS_REDIRECT=true

# Security - REQUIRED (min 32 chars)
JWT_SECRET_KEY=ReplaceWithYourStrongJwtKeyOfAtLeast32Chars

# Database - REQUIRED (full connection string)
DATABASE_URL=Host=your-db-host;Port=5432;Username=your-user;Password=your-password;Database=your-db;SSL Mode=Require;Trust Server Certificate=true

# SMTP - REQUIRED for email features
SMTP_SERVER=smtp.server.com
SMTP_PORT=587
SMTP_USER=user@domain.com
SMTP_PASSWORD=your-smtp-password
SMTP_ENABLE_SSL=true

# Frontend URL - REQUIRED for password reset links
FRONTEND_URL=https://your-frontend.example.com

# Python API - OPTIONAL if used by schedules
PYTHON_API_URL=https://your-python-api.example.com

# Encryption - REQUIRED for QR/tokens (Base64 strings)
ENCRYPTION_KEY=Base64Key32BytesMinimumExampleBase64Key==
ENCRYPTION_IV=Base64IV16BytesExample==
